<Window x:Class="DeckMiner.Gui.NewConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="建立新配置檔" Height="250" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Member Name -->
        <TextBlock Grid.Row="0" Text="成員名稱:" Margin="0,0,0,5"/>
        <TextBox x:Name="MemberNameTextBox" Grid.Row="1" TextChanged="MemberNameTextBox_TextChanged"/>

        <!-- Save Location -->
        <TextBlock Grid.Row="3" Text="儲存位置:" Margin="0,0,0,5"/>
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SavePathTextBox" Grid.Column="0" IsReadOnly="True" VerticalAlignment="Center"/>
            <Button x:Name="BrowseButton" Grid.Column="1" Content="瀏覽..." Width="80" Margin="5,0,0,0" Click="BrowseButton_Click"/>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="建立" Width="80" IsDefault="True" Click="CreateButton_Click" Margin="0,0,5,0"/>
            <Button Content="取消" Width="80" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
